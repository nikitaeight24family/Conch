新功能
• 悬浮窗重做：显示实时状态——动作、计时、令牌、正在运行的智能体、排队消息，不再显示旧回合的回复。只在确实有任务运行时才打开，并移除了容易误触的停止按钮。
• "+ 新对话"始终打开新对话。
• 顶栏显示对话的模型，而不是子智能体的模型。
• 智能体面板：每个智能体的状态、模型、令牌、工具次数和运行时长。
• 排队的消息在回合结束后一定会发出，包括按过停止之后。
• 最近任务中只保留一个卡片。回复到达时三次震动。
