Claude の次のプロンプト候補が実際に表示されるようになりました。

• 0.8.0 は Claude Code が無視する方法で候補を要求していました。今は CLI が想定する方法で有効にします（Claude Code 2.1.220 以降）。2 回目の返答から候補がキーボードの上に表示され、タップで入力欄に入ります。
• 上限が近いときは Claude Code 自身が候補を一時停止します。
