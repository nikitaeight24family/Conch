• The store no longer hides models this phone can run: the context window follows the device, and the built-in models are priced by their real architecture. A 4 GB phone goes from 1 model to 4; an 8 GB one from 8 to 11.
• Hugging Face results show an estimated size and hide what will not run; an over-budget download asks first.
• Losing Wi-Fi no longer takes this phone's own Linux with it.
• Every place that needs the phone bridge now leads to its setup.
