What's new
• Two new agents: xAI Grok and GitHub Copilot CLI — chat, sessions with their own titles, resume, model picker, in-app sign-in. Same first-class treatment as Claude, Codex and Gemini.
• Each agent keeps its own face: real logo, own spinner.
• Safety modes are audited now: installs pin the tested CLI version, every mode's flags are replayed through the CLI itself after install, and the shield sheet shows what was verified.
