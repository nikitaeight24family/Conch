• "This phone" is never "not connected" any more: Conch starts the phone's own Linux itself when you open it, and the dot shows it coming up instead of offline.
• The phone's shell reconnects on its own after Android restarts adbd — the handshake bug that made a paired phone look unpaired is fixed.
• The phone bridge no longer needs python3, so it also works inside the phone's own Linux; an out-of-date bridge now updates itself.
• The store never offers a model this device cannot run.
