What's new
• Fixed a vibration that could buzz on and on until you force-stopped the app, and one that fired the moment you pressed send while the answer was still coming.
• The agents' own background tasks no longer flood the chat — they are counted with the agents. Task rows in the conversation are now only its own.
• A queued message can no longer be lost when the session behind it had died.
• "+ new chat" opens a new chat from the agent switcher too.
