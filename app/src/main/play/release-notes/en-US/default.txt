• Local models now work as real agents, out of the box. Download any model and it drives the agent — running shell commands, reading files, inspecting your device — on the chat template that fits it. No setup: the app routes each model to the CLI that suits it.
• Each local model shows its own brand across the app, with a sessions filter chip per model.
• Steadier after a restart: no ghost "working" spinner, no false unread on finished local chats.
